// =============================================================
// pybind11 bindings for Millennium
// Auto-generated by dsl/gen_pybind.py — DO NOT EDIT BY HAND.
// =============================================================
#include "Millennium.hpp"
#include "pybind_common.hpp"

namespace py = pybind11;
using namespace EB::py_helpers;
using namespace Millennium;

PYBIND11_MODULE(Millennium_pb, m) {
    m.doc() = "exchbuf bindings for Millennium";

    // ----- enum MsgType -----
    {
        auto c = register_enum_struct<MsgType>(m, "MsgType");
        c.def_property_readonly_static("Heartbeat",
            [](py::object){ return py::cast(MsgType{MsgType::Heartbeat}); });
        c.def_property_readonly_static("Logon",
            [](py::object){ return py::cast(MsgType{MsgType::Logon}); });
        c.def_property_readonly_static("LogonResponse",
            [](py::object){ return py::cast(MsgType{MsgType::LogonResponse}); });
        c.def_property_readonly_static("Logout",
            [](py::object){ return py::cast(MsgType{MsgType::Logout}); });
        c.def_property_readonly_static("MissedMessageRequest",
            [](py::object){ return py::cast(MsgType{MsgType::MissedMessageRequest}); });
        c.def_property_readonly_static("MissedMessageRequestAck",
            [](py::object){ return py::cast(MsgType{MsgType::MissedMessageRequestAck}); });
        c.def_property_readonly_static("TransmissionComplete",
            [](py::object){ return py::cast(MsgType{MsgType::TransmissionComplete}); });
        c.def_property_readonly_static("Reject",
            [](py::object){ return py::cast(MsgType{MsgType::Reject}); });
        c.def_property_readonly_static("SystemStatus",
            [](py::object){ return py::cast(MsgType{MsgType::SystemStatus}); });
        c.def_property_readonly_static("NewOrder",
            [](py::object){ return py::cast(MsgType{MsgType::NewOrder}); });
        c.def_property_readonly_static("AmendOrder",
            [](py::object){ return py::cast(MsgType{MsgType::AmendOrder}); });
        c.def_property_readonly_static("CancelOrder",
            [](py::object){ return py::cast(MsgType{MsgType::CancelOrder}); });
        c.def_property_readonly_static("MassCancelRequest",
            [](py::object){ return py::cast(MsgType{MsgType::MassCancelRequest}); });
        c.def_property_readonly_static("ExecutionReport",
            [](py::object){ return py::cast(MsgType{MsgType::ExecutionReport}); });
        c.def_property_readonly_static("CancelReject",
            [](py::object){ return py::cast(MsgType{MsgType::CancelReject}); });
        c.def_property_readonly_static("MassCancelReport",
            [](py::object){ return py::cast(MsgType{MsgType::MassCancelReport}); });
        c.def_property_readonly_static("BusinessReject",
            [](py::object){ return py::cast(MsgType{MsgType::BusinessReject}); });
    }

    // ----- enum MissedMsgRespType -----
    {
        auto c = register_enum_struct<MissedMsgRespType>(m, "MissedMsgRespType");
        c.def_property_readonly_static("SUCESSFUL",
            [](py::object){ return py::cast(MissedMsgRespType{MissedMsgRespType::SUCESSFUL}); });
        c.def_property_readonly_static("RECOVERY_REQUEST_LIMIT_REACHED",
            [](py::object){ return py::cast(MissedMsgRespType{MissedMsgRespType::RECOVERY_REQUEST_LIMIT_REACHED}); });
        c.def_property_readonly_static("INVALID_APPID",
            [](py::object){ return py::cast(MissedMsgRespType{MissedMsgRespType::INVALID_APPID}); });
        c.def_property_readonly_static("SERVICE_UNAVAILABLE",
            [](py::object){ return py::cast(MissedMsgRespType{MissedMsgRespType::SERVICE_UNAVAILABLE}); });
    }

    // ----- enum MissedMsgRptType -----
    {
        auto c = register_enum_struct<MissedMsgRptType>(m, "MissedMsgRptType");
        c.def_property_readonly_static("DOWNLOAD_COMPLETE",
            [](py::object){ return py::cast(MissedMsgRptType{MissedMsgRptType::DOWNLOAD_COMPLETE}); });
        c.def_property_readonly_static("MESSAGE_LIMIT_REACHED",
            [](py::object){ return py::cast(MissedMsgRptType{MissedMsgRptType::MESSAGE_LIMIT_REACHED}); });
        c.def_property_readonly_static("SERVICE_UNAVAILABLE",
            [](py::object){ return py::cast(MissedMsgRptType{MissedMsgRptType::SERVICE_UNAVAILABLE}); });
    }

    // ----- enum AppID -----
    {
        auto c = register_enum_struct<AppID>(m, "AppID");
        c.def_property_readonly_static("SYSTEM_SUSPENDED_UNKNOWN_INSTRUMENT",
            [](py::object){ return py::cast(AppID{AppID::SYSTEM_SUSPENDED_UNKNOWN_INSTRUMENT}); });
        c.def_property_readonly_static("PARTITION1",
            [](py::object){ return py::cast(AppID{AppID::PARTITION1}); });
        c.def_property_readonly_static("PARTITION2",
            [](py::object){ return py::cast(AppID{AppID::PARTITION2}); });
        c.def_property_readonly_static("PARTITION3",
            [](py::object){ return py::cast(AppID{AppID::PARTITION3}); });
        c.def_property_readonly_static("PARTITION4",
            [](py::object){ return py::cast(AppID{AppID::PARTITION4}); });
    }

    // ----- enum AppStatus -----
    {
        auto c = register_enum_struct<AppStatus>(m, "AppStatus");
        c.def_property_readonly_static("RECOVERY_SERVICE_RESUMED",
            [](py::object){ return py::cast(AppStatus{AppStatus::RECOVERY_SERVICE_RESUMED}); });
        c.def_property_readonly_static("RECOVERY_SERVICE_NOT_AVAILABLE",
            [](py::object){ return py::cast(AppStatus{AppStatus::RECOVERY_SERVICE_NOT_AVAILABLE}); });
    }

    // ----- message MsgHeader -----
    {
        using H = Holder<MsgHeader>;
        auto c = py::class_<H>(m, "MsgHeader")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return MsgHeader::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return MsgHeader::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
    }

    // ----- message Logon -----
    {
        using H = Holder<Logon>;
        auto c = py::class_<H>(m, "Logon")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return Logon::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return Logon::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("userName",
            [](H& h) { return fls_to_string(h.msg().userName); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().userName, v); });
        c.def_property("password",
            [](H& h) { return fls_to_string(h.msg().password); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().password, v); });
        c.def_property("newPassword",
            [](H& h) { return fls_to_string(h.msg().newPassword); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().newPassword, v); });
        c.def_property("messageVersion",
            [](H& h) { return h.msg().messageVersion.to_int(); },
            [](H& h, decltype(h.msg().messageVersion.to_int()) v) { h.msg().messageVersion = v; });
    }

    // ----- message LogonResponse -----
    {
        using H = Holder<LogonResponse>;
        auto c = py::class_<H>(m, "LogonResponse")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return LogonResponse::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return LogonResponse::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("rejectCode",
            [](H& h) { return h.msg().rejectCode.to_int(); },
            [](H& h, decltype(h.msg().rejectCode.to_int()) v) { h.msg().rejectCode = v; });
        c.def_property("passwordExpiryDayCount",
            [](H& h) { return fls_to_string(h.msg().passwordExpiryDayCount); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().passwordExpiryDayCount, v); });
    }

    // ----- message Logout -----
    {
        using H = Holder<Logout>;
        auto c = py::class_<H>(m, "Logout")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return Logout::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return Logout::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("reason",
            [](H& h) { return fls_to_string(h.msg().reason); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().reason, v); });
    }

    // ----- message Heartbeat -----
    {
        using H = Holder<Heartbeat>;
        auto c = py::class_<H>(m, "Heartbeat")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return Heartbeat::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return Heartbeat::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
    }

    // ----- message MissedMessageRequest -----
    {
        using H = Holder<MissedMessageRequest>;
        auto c = py::class_<H>(m, "MissedMessageRequest")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return MissedMessageRequest::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return MissedMessageRequest::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("appID",
            [](H& h) { return h.msg().appID.to_int(); },
            [](H& h, decltype(h.msg().appID.to_int()) v) { h.msg().appID = v; });
        c.def_property("lastMsgSeqNum",
            [](H& h) { return h.msg().lastMsgSeqNum.to_int(); },
            [](H& h, decltype(h.msg().lastMsgSeqNum.to_int()) v) { h.msg().lastMsgSeqNum = v; });
    }

    // ----- message MissedMessageRequestAck -----
    {
        using H = Holder<MissedMessageRequestAck>;
        auto c = py::class_<H>(m, "MissedMessageRequestAck")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return MissedMessageRequestAck::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return MissedMessageRequestAck::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("responseType",
            [](H& h) { return h.msg().responseType; },
            [](H& h, MissedMsgRespType v) { h.msg().responseType = v; });
    }

    // ----- message TransmissionComplete -----
    {
        using H = Holder<TransmissionComplete>;
        auto c = py::class_<H>(m, "TransmissionComplete")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return TransmissionComplete::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return TransmissionComplete::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("responseType",
            [](H& h) { return h.msg().responseType; },
            [](H& h, MissedMsgRptType v) { h.msg().responseType = v; });
    }

    // ----- message Reject -----
    {
        using H = Holder<Reject>;
        auto c = py::class_<H>(m, "Reject")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return Reject::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return Reject::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("rejectCode",
            [](H& h) { return h.msg().rejectCode.to_int(); },
            [](H& h, decltype(h.msg().rejectCode.to_int()) v) { h.msg().rejectCode = v; });
        c.def_property("rejectReason",
            [](H& h) { return fls_to_string(h.msg().rejectReason); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().rejectReason, v); });
        c.def_property("rejectedMessageType",
            [](H& h) { return h.msg().rejectedMessageType.to_int(); },
            [](H& h, decltype(h.msg().rejectedMessageType.to_int()) v) { h.msg().rejectedMessageType = v; });
        c.def_property("clientOrderID",
            [](H& h) { return fls_to_string(h.msg().clientOrderID); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().clientOrderID, v); });
    }

    // ----- message SystemStatus -----
    {
        using H = Holder<SystemStatus>;
        auto c = py::class_<H>(m, "SystemStatus")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return SystemStatus::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return SystemStatus::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("appID",
            [](H& h) { return h.msg().appID; },
            [](H& h, AppID v) { h.msg().appID = v; });
        c.def_property("appStatus",
            [](H& h) { return h.msg().appStatus; },
            [](H& h, AppStatus v) { h.msg().appStatus = v; });
    }

    // ----- message BusinessReject -----
    {
        using H = Holder<BusinessReject>;
        auto c = py::class_<H>(m, "BusinessReject")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return BusinessReject::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return BusinessReject::wire_size(s.data());
                })
            ;
        c.def_property("startOfMsg",
            [](H& h) { return h.msg().startOfMsg.to_int(); },
            [](H& h, decltype(h.msg().startOfMsg.to_int()) v) { h.msg().startOfMsg = v; });
        c.def_property("length",
            [](H& h) { return h.msg().length.to_int(); },
            [](H& h, decltype(h.msg().length.to_int()) v) { h.msg().length = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("appID",
            [](H& h) { return h.msg().appID; },
            [](H& h, AppID v) { h.msg().appID = v; });
        c.def_property("sequenceNo",
            [](H& h) { return h.msg().sequenceNo.to_int(); },
            [](H& h, decltype(h.msg().sequenceNo.to_int()) v) { h.msg().sequenceNo = v; });
        c.def_property("rejectCode",
            [](H& h) { return h.msg().rejectCode.to_int(); },
            [](H& h, decltype(h.msg().rejectCode.to_int()) v) { h.msg().rejectCode = v; });
        c.def_property("clientOrderID",
            [](H& h) { return fls_to_string(h.msg().clientOrderID); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().clientOrderID, v); });
        c.def_property("orderID",
            [](H& h) { return fls_to_string(h.msg().orderID); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().orderID, v); });
        c.def_property("transactTime",
            [](H& h) { return h.msg().transactTime.to_int(); },
            [](H& h, decltype(h.msg().transactTime.to_int()) v) { h.msg().transactTime = v; });
        c.def_property("reserved1",
            [](H& h) { return fls_to_string(h.msg().reserved1); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().reserved1, v); });
    }

}


