// =============================================================
// pybind11 bindings for sandbox1
// Auto-generated by dsl/gen_pybind.py — DO NOT EDIT BY HAND.
// =============================================================
#include "sandbox1.hpp"
#include "pybind_common.hpp"

namespace py = pybind11;
using namespace EB::py_helpers;
using namespace sandbox1;

PYBIND11_MODULE(sandbox1_pb, m) {
    m.doc() = "exchbuf bindings for sandbox1";

    // ----- enum E1 -----
    {
        auto c = register_enum_struct<E1>(m, "E1");
        c.def_property_readonly_static("F1",
            [](py::object){ return py::cast(E1{E1::F1}); });
        c.def_property_readonly_static("F2",
            [](py::object){ return py::cast(E1{E1::F2}); });
    }

    // ----- enum E2 -----
    {
        auto c = register_enum_struct<E2>(m, "E2");
        c.def_property_readonly_static("F1",
            [](py::object){ return py::cast(E2{E2::F1}); });
        c.def_property_readonly_static("F2",
            [](py::object){ return py::cast(E2{E2::F2}); });
    }

    // ----- bitfield BF1 -----
    {
        auto c = py::class_<BF1>(m, "BF1")
            .def(py::init<>())
            .def_readwrite("bits", &BF1::bits);
        c.def_property("E1",
            [](BF1& bf) { return bf.getE1(); },
            [](BF1& bf, E1 v) { bf.setE1(v); });
        c.def_property("E2",
            [](BF1& bf) { return bf.getE2(); },
            [](BF1& bf, E2 v) { bf.setE2(v); });
    }

    // ----- enum MsgType -----
    {
        auto c = register_enum_struct<MsgType>(m, "MsgType");
        c.def_property_readonly_static("New",
            [](py::object){ return py::cast(MsgType{MsgType::New}); });
        c.def_property_readonly_static("Amend",
            [](py::object){ return py::cast(MsgType{MsgType::Amend}); });
        c.def_property_readonly_static("Cancel",
            [](py::object){ return py::cast(MsgType{MsgType::Cancel}); });
    }

    // ----- message Head -----
    {
        using H = Holder<Head>;
        auto c = py::class_<H>(m, "Head")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return Head::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return Head::wire_size(s.data());
                })
            ;
        c.def_property("len",
            [](H& h) { return h.msg().len.to_int(); },
            [](H& h, decltype(h.msg().len.to_int()) v) { h.msg().len = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
    }

    // ----- message RptGrp -----
    {
        using H = Holder<RptGrp>;
        auto c = py::class_<H>(m, "RptGrp")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return RptGrp::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return RptGrp::wire_size(s.data());
                })
            ;
        c.def_property("key",
            [](H& h) { return h.msg().key.to_int(); },
            [](H& h, decltype(h.msg().key.to_int()) v) { h.msg().key = v; });
        c.def_property("value",
            [](H& h) { return fls_to_string(h.msg().value); },
            [](H& h, const std::string& v) { fls_from_string(h.msg().value, v); });
    }

    // ----- enum AppendageType -----
    {
        auto c = register_enum_struct<AppendageType>(m, "AppendageType");
        c.def_property_readonly_static("ClearingAccount",
            [](py::object){ return py::cast(AppendageType{AppendageType::ClearingAccount}); });
        c.def_property_readonly_static("ClearingAccountType",
            [](py::object){ return py::cast(AppendageType{AppendageType::ClearingAccountType}); });
        c.def_property_readonly_static("ClearingFirm",
            [](py::object){ return py::cast(AppendageType{AppendageType::ClearingFirm}); });
    }

    // ----- message Entry -----
    {
        using H = Holder<Entry>;
        auto c = py::class_<H>(m, "Entry")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return Entry::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return Entry::wire_size(s.data());
                })
            ;
        c.def_property("len",
            [](H& h) { return h.msg().len.to_int(); },
            [](H& h, decltype(h.msg().len.to_int()) v) { h.msg().len = v; });
        c.def_property("tag",
            [](H& h) { return h.msg().tag; },
            [](H& h, AppendageType v) { h.msg().tag = v; });
        c.def_property_readonly("value",
            py::cpp_function(
                [](H& h) { return h.msg().value(); },
                py::keep_alive<0, 1>()));
    }

    // ----- message NewOrder -----
    {
        using H = Holder<NewOrder>;
        register_block_view<H, RptGrp>(
            m, "NewOrder_grp_View");
        auto c = py::class_<H>(m, "NewOrder")
            .def_static("allocate", &H::allocate, py::arg("cap") = DEFAULT_CAP)
            .def_static("from_bytes", &H::from_bytes)
            .def("__bytes__", &H::to_bytes)
            .def("to_bytes", &H::to_bytes)
            .def("size", &H::size)
            .def("capacity", &H::capacity)
            .def_static("fixed_size", []() { return NewOrder::fixed_size(); })
            .def_static("wire_size",
                [](py::bytes b) {
                    std::string s(b);
                    return NewOrder::wire_size(s.data());
                })
            ;
        c.def_property("len",
            [](H& h) { return h.msg().len.to_int(); },
            [](H& h, decltype(h.msg().len.to_int()) v) { h.msg().len = v; });
        c.def_property("msgType",
            [](H& h) { return h.msg().msgType; },
            [](H& h, MsgType v) { h.msg().msgType = v; });
        c.def_property("price",
            [](H& h) { return h.msg().price.to_double(); },
            [](H& h, double v) { h.msg().price = v; });
        c.def_property("qty",
            [](H& h) { return h.msg().qty.to_int(); },
            [](H& h, decltype(h.msg().qty.to_int()) v) { h.msg().qty = v; });
        c.def_property("sz",
            [](H& h) { return h.msg().sz.to_int(); },
            [](H& h, decltype(h.msg().sz.to_int()) v) { h.msg().sz = v; });
        c.def_property("plen",
            [](H& h) { return h.msg().plen.to_int(); },
            [](H& h, decltype(h.msg().plen.to_int()) v) { h.msg().plen = v; });
        c.def_property("pmap",
            [](H& h) { return h.msg().pmap.to_int(); },
            [](H& h, decltype(h.msg().pmap.to_int()) v) { h.msg().pmap = v; });
        c.def_property_readonly("grp",
            py::cpp_function(
                [](H& h) {
                    return BlockRefView<H, RptGrp>(h,
                        [](H& hh) { return hh.msg().grp(); });
                }, py::keep_alive<0, 1>()));
    }

}


