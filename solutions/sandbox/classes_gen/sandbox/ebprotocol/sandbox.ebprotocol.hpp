#pragma once
//this file is generated by exchbuf(https://github.com/ChrisYan6807/exchbuf), please do NOT edit it manually!!!
#include "eb_common.hpp"

namespace sandbox {
using namespace EB::common;

using U8 = LittleEndian<uint8_t, std::numeric_limits<uint8_t>::min(), std::numeric_limits<uint8_t>::max(), 0_u8>;
using U16 = LittleEndian<uint16_t, std::numeric_limits<uint16_t>::min(), std::numeric_limits<uint16_t>::max(), 0>;

//eb comment

//cpp comment

struct MsgType {
    using value_type = char;
    enum Enum : value_type {
        new = 'a',
        amend = 'b',
    };
    static constexpr size_t size = 1;
    static constexpr char* name() {return "MsgType";}
    static constexpr value_type min_value = std::min<value_type>({static_cast<char>('a'), static_cast<char>('b'), });
    static constexpr value_type max_value = std::max<value_type>({static_cast<char>('a'), static_cast<char>('b'), });
    constexpr MsgType():value_{max_value} {}
    constexpr explicit MsgType(char v):value_{v} {}
    constexpr MsgType(Enum v):value_{v} {}
    constexpr bool operator==(const MsgType& rhs) const {return value_ == rhs.value_;}
    constexpr bool operator!=(const MsgType& rhs) const {return value_ != rhs.value_;}
    constexpr bool operator!=(Enum v) const {return value_ != v;}
    constexpr Enum value() const {return value_;}
    constexpr char raw_value() const {return static_cast<char>(value_);}
    constexpr void raw_value(char v) {value_ = Enum(v);}
    constexpr void set(Enum v) {value_ = v;}
    constexpr MsgType& operator=(Enum v) {value_ == v;return *this;}
    constexpr MsgType& operator=(const MsgType& rhs) = default;
    constexpr int length() const {return sizeof(value_);}
    constexpr const std::string_view view() const {
        switch(value_) {
            case Enum::new: return "new";
            case Enum::amend: return "amend";
        }
        return "";
    }
    Enum value_{max_value};
};

template <typename ostreamT>
inline ostreamT& operator<<(ostreamT& os, const MsgType& v){
    os << v.view();
    return os;
}

struct em2 {
    using value_type = int8_t;
    enum Enum : value_type {
        c = 1,
        e = 'z',
        f = 'g',
    };
    static constexpr size_t size = 1;
    static constexpr char* name() {return "em2";}
    static constexpr value_type min_value = std::min<value_type>({static_cast<int8_t>(1), static_cast<int8_t>('z'), static_cast<int8_t>('g'), });
    static constexpr value_type max_value = std::max<value_type>({static_cast<int8_t>(1), static_cast<int8_t>('z'), static_cast<int8_t>('g'), });
    constexpr em2():value_{max_value} {}
    constexpr explicit em2(int8_t v):value_{v} {}
    constexpr em2(Enum v):value_{v} {}
    constexpr bool operator==(const em2& rhs) const {return value_ == rhs.value_;}
    constexpr bool operator!=(const em2& rhs) const {return value_ != rhs.value_;}
    constexpr bool operator!=(Enum v) const {return value_ != v;}
    constexpr Enum value() const {return value_;}
    constexpr int8_t raw_value() const {return static_cast<int8_t>(value_);}
    constexpr void raw_value(int8_t v) {value_ = Enum(v);}
    constexpr void set(Enum v) {value_ = v;}
    constexpr em2& operator=(Enum v) {value_ == v;return *this;}
    constexpr em2& operator=(const em2& rhs) = default;
    constexpr int length() const {return sizeof(value_);}
    constexpr const std::string_view view() const {
        switch(value_) {
            case Enum::c: return "c";
            case Enum::e: return "e";
            case Enum::f: return "f";
        }
        return "";
    }
    Enum value_{max_value};
};

template <typename ostreamT>
inline ostreamT& operator<<(ostreamT& os, const em2& v){
    os << v.view();
    return os;
}

struct bf {
    void setem2(em2 v) {
        auto rv = v.raw_value();
        bits &= clear_mask<int16_t, 0, 1>;
        bits |= rv;
    }

    em2 getem2() {
        auto v = bits&get_mask<int16_t, 0, 1>;
        return int16_t(v);
    }

    void setMsgType(MsgType v) {
        auto rv = v.raw_value();
        rv <<= 1;
        bits &= clear_mask<int16_t, 1, 2>;
        bits |= rv;
    }

    MsgType getMsgType() {
        auto v = bits&get_mask<int16_t, 1, 2>;
        v >>= 1;
        return int16_t(v);
    }

    void clear() {
        bits = 0;
    }
    int16_t bits;
 };

template<typename ostreamT>
ostreamT& operator<<(ostreamT& os, const bf& v) {
    os << "em2=" << v.getem2();
    os << "|MsgType=" << v.getMsgType();
}




} // end of namespace sandbox


